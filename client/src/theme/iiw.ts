/** Brand constants — keep in sync with `global.css` `--iiw-blue`. */
export const IIW_BLUE = "#0066a2";

/** Secondary stroke for `react-loader-spinner` Oval (readable in light + dark). */
export const IIW_LOADER_SECONDARY = "rgba(0, 102, 162, 0.2)";
